"""
config.py
─────────
spotlight_core 및 관련 모듈에서 공유하는 설정 상수.
"""

RPI_WS_URL   = "ws://192.168.137.114:8765"  # RPi WebSocket 주소 (Wi-Fi)
WS_PORT      = 8765                         # Electron 앱과 통신할 포트
FRAME_WIDTH  = 1920                         # 카메라 해상도 (1080p)
FRAME_HEIGHT = 1080

X_DEAD_ZONE  = 300                          # 팬 데드존 (200@1280 → 300@1920 비례 스케일)
Y_DEAD_ZONE  = 150                          # 틸트 데드존 (100@720 → 150@1080 비례 스케일)

# ─── PID 모터 제어 파라미터 ───────────────────────────────
PID_KP           = 0.20    # 비례 게인 — 오차 크기에 비례한 즉각 보정
PID_KI           = 0.0001  # 적분 게인 — 지속 오차 누적 보정 (데드존 경계 처리)
PID_KD           = 0.08    # 미분 게인 — 오차 변화 속도에 제동 (오버슈트·떨림 억제)
PID_OUTPUT_LIMIT = 15.0    # PID 1회 출력 최대 각도 변화 (±도) — 빠른 추적 핵심
EMA_ALPHA        = 0.25    # EMA 스무딩 팩터 (낮을수록 부드러움, 높을수록 응답 빠름)

# ─── 추적 안정화 파라미터 ─────────────────────────────────
MAX_JUMP_PX      = 400     # 이 픽셀 이상 좌표가 급변하면 점프로 간주해 무시
MIN_SEND_INTERVAL = 0.033  # RPi 최소 전송 간격 (초) — 최대 30fps 상한
